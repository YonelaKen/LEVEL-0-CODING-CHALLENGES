function calculateArea(a,b,c){
    //This line is the sum that defines the calculation of the area
    var area = 0.5 * (a + b + c);
    //This funtion returns the value of the area
    return area;
  }
  console.log(calculateArea(3,4,5));